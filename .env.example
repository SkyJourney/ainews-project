# 变量名模板，不含真实值。本机实际部署时，真实 .env 放在
# /Volumes/Docker/compose/ainews-service/.env（不进本仓库 git）。

# ── Postgres（ainews_content 库）──────────────────────────
POSTGRES_DB=ainews_content
POSTGRES_USER=ainews
POSTGRES_PASSWORD=
POSTGRES_PORT=15432

# ── Temporal ──────────────────────────────────────────────
TEMPORAL_GRPC_PORT=7233
TEMPORAL_UI_PORT=8088

# ── 自建 LiteLLM 网关 ──────────────────────────────────────
# BASE_URL 必须带协议前缀 /openai/v1（网关按协议分路径挂载，裸 /v1 会落到
# 网关默认路由上，不是预期的 OpenAI 协议路径——排查连通性问题时先检查这一点）。
LITELLM_BASE_URL=
LITELLM_API_KEY=

# ── backend 镜像版本（docker compose build 打的 tag）───────
BACKEND_IMAGE_TAG=0.1.0

# ── M6：前端（Astro SSR + Node adapter）──────────────────────
FRONTEND_IMAGE_TAG=0.1.0
WEB_PORT=4321

# ── M7：Postgres 每日备份保留天数 ──────────────────────────
BACKUP_RETENTION_DAYS=14
