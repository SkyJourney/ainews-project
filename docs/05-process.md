# 文档体系与推进流程

> 本文档说明 `docs/` 下各文件的定位与相互关系，以及新会话该如何使用这套文档推进开发。同时承担"进展日志"的角色——见 §4，每次有实质性进展都要在这里追加一行记录。

## 1. 文档体系全景

| 文档 | 定位 | 什么时候读 |
|---|---|---|
| [`00-overview.md`](./00-overview.md) | 起点文档：问题陈述、目标、结论摘要表、架构总图 | 任何新会话的第一份必读 |
| [`01-document-database-research.md`](./01-document-database-research.md) | 专项调研：文档数据库选型完整推理过程 | 需要理解"为什么是 Postgres 不是 MongoDB/SQLite"时才展开读 |
| [`02-pipeline-orchestration-research.md`](./02-pipeline-orchestration-research.md) | 专项调研：编排框架选型完整推理过程 | 需要理解"为什么是 Temporal 不是纯 Celery"时才展开读 |
| [`03-architecture-proposal.md`](./03-architecture-proposal.md) | 整体架构方案：数据模型 SQL、Phase 映射表、部署拓扑、迁移路径、待决问题 | 需要具体 schema / 部署细节 / 待决问题时查阅 |
| [`04-roadmap.md`](./04-roadmap.md) | **新会话的唯一权威入口**：不耦合旧 AInews 目录的功能清单（业务规则完整提炼，§2）+ 里程碑设计（§4） | 任何新会话的第二份必读；开始某个里程碑前必读对应 §2 小节 |
| `05-process.md`（本文档） | 文档体系导航 + 推进流程 + 进展日志 | 想知道"现在推进到哪了 / 这套文档该怎么用"时查阅 |
| [`milestones/`](./milestones/) | 每个里程碑的执行细化版：从 04 §4 展开成可勾选任务清单 + 验收标准，**随开发推进持续补充真实实现细节** | 具体动手做某个里程碑时，逐项对照执行 |

关系一句话总结：00 是门面，01/02/03 是调研证据，04 是权威规格，05（本文档）是使用说明书 + 时间线，`milestones/` 是把 04 的规格拆解成可执行、可勾选、会随实现不断补充细节的活文档。

## 2. 新会话该怎么用这套文档

1. 先读 `00-overview.md`，建立整体认知（问题是什么、目标是什么、架构长什么样）。
2. 再读 `04-roadmap.md` §1-§3（核心原则 + 推进顺序），了解当前处于服务化重构的哪个阶段。
3. 查 `milestones/README.md` 的状态表，确认当前实际推进到哪个里程碑（不要假设，以表格状态为准；状态表和各里程碑文件顶部的"状态"字段应保持一致）。
4. 打开对应里程碑文件，同时对照 `04-roadmap.md` §2 中该里程碑覆盖的功能清单小节，逐项推进任务清单。
5. 遇到 checklist 没覆盖到的边界情况，回 `04-roadmap.md` §2 找权威表述；`01`/`02`/`03` 用于查具体选型理由或 schema 细节。

## 3. 文档维护规则（所有会话都要遵守）

### 3.1 里程碑文档需要跟随实现同步更新

`docs/milestones/*.md` 当前只是从 04 提炼的**框架性**任务清单（因为项目尚未开始写代码），不包含任何真实实现细节。**从有实质性代码/配置产出的那一刻起**：

- 完成一项任务清单条目时，把 `- [ ]` 改成 `- [x]`，并在该条目下追加一小段"落地方式"说明——具体用了什么库/表结构/关键决策/踩过的坑，而不是只打勾了事。目标是下一个会话看一眼里程碑文件就知道"这件事具体是怎么做的"，不需要再翻代码或翻聊天记录。
- 里程碑文件顶部的"状态"字段（未开始/进行中/已完成）随实际进度更新，`milestones/README.md` 的状态表格必须同步更新，保持两处一致，不能一处更新一处遗漏。
- 验收标准全部勾选通过、且经过实际验证后，才允许把状态改为"已完成"并进入下一个里程碑——04 §3 的既定原则："前一个不达标不进入下一个"，milestones 文档的状态字段就是这条原则的具体落点。

### 3.2 本文档需要维护"进展日志"（§4）

每次有实质性进展（不要求是整个里程碑完成，某个任务清单条目完成、某个关键实现决策定下来都算），在 §4 追加一行记录。日志只记"发生了什么、为什么"这种时间线信息，具体怎么实现的细节写进对应里程碑文件，不要在这里重复展开。

## 4. 进展日志

| 日期 | 里程碑 | 事件 |
|---|---|---|
| 2026-07-04 | - | 完成 00-04 架构调研与 roadmap；创建 `docs/milestones/` 全部 10 份里程碑框架文档 + 索引；初始化项目级 `CLAUDE.md`；新建本文档 |
| 2026-07-04 | M0 | backend 骨架落地（conda_env+requirements.txt / Dockerfile / Alembic 迁移 / Temporal worker / Celery beat），两份 docker-compose.yml（仓库通用版 `infra/` + 本机部署版 `/Volumes/Docker/compose/ainews-service/`，项目在本机命名为 `ainews-service` 以区分旧系统的 `ainews`）。六容器（db/redis/temporal/temporal-ui/temporal-worker/celery-worker/celery-beat）已实际起服务并持续运行，Alembic 建表验证通过，手动触发 hello-world workflow 验证 Temporal Web UI 执行历史 + Postgres 写入均正常。唯一未完成项：LiteLLM 网关连通性测试，待真实 endpoint/key 填入 `/Volumes/Docker/compose/ainews-service/.env` 后补跑，不阻塞后续里程碑。 |
| 2026-07-04 | M0 | 补充 LLM 客户端依赖（`openai`+`instructor`+`pydantic`，此前 requirements.txt 遗漏，M0 的 LiteLLM 连通性测试其实卡在缺客户端库这一步）；实际创建本机 `ainews-service` conda 环境，用 pip 解析器探查全部依赖的兼容最新版本并精确 pin（不再用范围），`pip check` 验证无冲突；backend 镜像重建、六容器重启验证仍正常。 |
| 2026-07-04 | M0 | **M0 收尾，状态改为已完成**。真实 LiteLLM 网关凭据填入 `/Volumes/Docker/compose/ainews-service/.env`（`base_url` 需带 `/openai/v1` 协议前缀，裸 `/v1` 会落到网关另一套走 Anthropic 兼容端点的模型注册表，排查时曾误判为网关路由问题，实际是 base_url 少了协议前缀）；新增 `backend/config/models.yaml` 记录网关支持的 17 个模型（配置数据）。 |
| 2026-07-04 | M0 | **架构决策：`tool_choice` 统一用 `auto`，放弃强制模式**（更新 04-roadmap.md §2.8）。背景：强制 tool_choice 在这台网关上因模型而异——GLM/Kimi 部分型号直接支持，Qwen 商业系列加 `extra_body={"enable_thinking": False}` 后支持，DeepSeek v4（pro/flash）两种协议路径 + 传或不传各种 thinking 参数均不支持（穷尽测试后确认是模型恒定 thinking 开启，与协议选型无关，参考了 `/Volumes/Projects/Autotest_Platform` 里 `llm_client.py` 的既有实践，其生产配置同样只验证过 Qwen 未验证过 DeepSeek）。最终决定不做按模型分支的调用逻辑，统一 `auto` + Instructor 校验 + Temporal 重试兜底；实测 `auto` 模式下 DeepSeek v4 的 reasoning 开销很小（约 30 tokens），不是性能妥协。`models.yaml` 的 `tool_choice_forced` 字段保留作参考，不再是调用策略依据。 |
| 2026-07-04 | M1 | **M1 完成，状态改为已完成**：`openai-rss` 单源端到端全链路（preflight→fetch→filter→enrich×M child workflow fan-out→aggregate→write）真实跑通，经真实 Temporal Server + Worker 执行（非直接函数调用），16 篇文章 0 失败全部写入 `documents` 表。过程中三处修正：① Instructor 的 `Mode.TOOLS` 会忽略显式 `tool_choice="auto"`，改用 `Mode.JSON`（决策记录见 decisions.md）；② openai.com 文章页挂 Cloudflare 反爬挑战，参考旧项目 `fetch-with-assets.py`/`news-originalizer.md` 提前把 04 §2.4 状态②（Jina Reader 兜底）实现到 M1，`articles` 表新增 `fetch_channel`/`published_at` 两列（migrations 0002/0003）；③ `deepseek-v4-flash` 偶发把答案整个写进 `reasoning_content`、`content` 留空（DeepSeek 官方文档承认的已知问题），`max_retries` 从 0 改为 1 后稳定修复。**路线图调整**：M2-M4 从"逐个里程碑独立验收"改为"合并成一个连续阶段推进"（04-roadmap.md §3 已更新），理由是 M1 验证的是"新架构管道形状对不对"这类高不确定性工作，M2-M4 是从老系统迁移已验证规则，不确定性低。 |
| 2026-07-04 | M2+M3+M4 | **M2+M3+M4 合并阶段完成，三个里程碑状态均改为已完成**：分 Stage A-E 推进。Stage A（M2 Filter）补齐同批次去重③④、跨源论文去重、信噪比过滤表，新增 Postgres 表 `url_index`（migration 0004）落地跨日去重（替代旧系统 JSON 文件方案）。Stage B（M3 全源接入）补全 14 条源配置，`fetch_activity` 新增 api/webfetch/script 三分支，新增 `source_health` 表（migration 0005）落地健康状态机，`AInewsPipelineWorkflow` 从单源单次调用改造成按活跃源 fan-out。Stage C 用真实 14 源数据复验 M2 规则，修正跨源论文去重的同源误判。Stage D（M4 Enrich）补齐 Fallback A（Playwright 无头浏览器，backend 镜像新增 Chromium）+ Fallback B 占位、配图分级渲染（新增 Docker 卷 + `ainews-media://` 自定义 scheme 引用）、翻译完整性机械校验、富元数据抽取（migration 0006 补 `word_count`/`translation_fallback_notice`），并修复安全审查发现的 SSRF 风险（`_assert_public_url`/`_safe_get`）。Stage E 验收：全 14 源真实批次 168 篇文章，**enrich_failed=0，覆盖率 100%**，达成 M4 核心验收标准。过程中还修正了 trafilatura `include_images` 默认关闭、arxiv.org 引用工具区块与站点跟踪像素拖累翻译校验（降级率从 29% 修到 9.5%）等实测踩坑，详见 decisions.md。 |
| 2026-07-05 | M4（追加） | **翻译降级问题深度排查完成**：M4 验收后用户额外要求专项 investigation，目标"复杂文章翻译也做到无降级"。给 `translate_activity` 加逐块诊断日志跑真实批次（146 篇 17 篇失败），归成 5 类根因，针对性修复 3 类——分块级数据/噪声行识别（跳过噪声翻译+从 CJK 占比分母排除数据行）、单块重试上限从 1 次加到 2 次、openai.com 头部导航与相关文章推荐区块清理、通用重复段落去重（一次覆盖 a16z 侧栏与 arxiv 许可证图标块）、本地图片自定义 scheme 补漏不计入分母；新增独立复审机制（机械校验不通过时另开一次 LLM 调用核对是否真的不完整，减少专有名词/数据密度导致的误杀，机械校验仍是唯一主判据）。另外 2 类根因（品牌名密度临界失败、state-of-ai 历史 Edition 落地页）确认不需要修——前者旧项目也没有豁免逻辑，后者跨日去重已能自动处理。**最终全新全量批次验收：144 篇，0 enrich_failed，0 篇最终降级**（原始诊断批次 11.6% → 0%）。详见 `docs/milestones/M4-enrich.md` 与 decisions.md。 |
| 2026-07-05 | M5 | **M5（Aggregate 阶段完整化）完成**：补齐 04 §2.6 "Original"归档层这个此前被 zettel 顶替、一直没做的缺口——每篇文章都建 `original`（真正的完整译文归档），只有聚类判断 `zettel_worthy` 的文章才额外建/复用 `zettel`（原子笔记，三级复用判断）。实现 Topic 追加铁律（首建/追加分支，`documents` 整行 UPSERT 语义下靠读旧文档+计算合并结果落地"绝不整体重写"）、Daily 五种情形分类、Digest 五项自检、Tags 四轴打标，`tags`/`links` 两张建表以来从未写入的表首次真正启用。真实批次验证：136+ 篇一次性聚类/打标调用被 max_tokens 截断，改成分块调用；zettel_worthy 首次实测 48% 远超"3-10 张"软性指标，收紧 prompt 后降到符合区间；`aggregate_activity`/`write_activity` 的 Temporal 超时沿用 M1 时代 30 秒遗留值不够用，调至 180s/90s；发现并修复了一个真实的悬空引用风险（`url_index.zettel_id` 指向的文档实际不存在时会导致 `links` 外键违反，整批写入失败）。工程化收敛补项：新增 pytest 测试基建（`backend/tests/`，48 个用例，覆盖 M5 新逻辑 + 回溯覆盖 M2/M4 已有纯函数），全部 mock 数据库/LLM，不连真实环境即可快速验证分支逻辑。最终真实全量批次：146 篇保留、145 篇 enrich 成功、165 条文档写入（145 original+8 zettel+10 topic+1 daily+1 digest）。详见 `docs/milestones/M5-aggregate.md` 与 decisions.md。 |
| 2026-07-05 | M6 | **M6（前端上线）完成**：前置调研发现旧前端（`/Volumes/Projects/AInews/web/frontend`）实际已是 Astro 7.0.5（不是计划里写的"升级到6"），核实 SSR 所需能力均已具备后改为"维持 7.0.5，只做 SSR 配置改造"。整体复制旧前端到新仓库 `frontend/`，只换两处：① 自建 `postgres-loader.ts` 实现 `astro/loaders` 的 Live Loader 接口，请求时查 `documents` 表（关键细节：Live entry 不走 build-time 渲染管线，需手动调用 `createMarkdownProcessor` 生成 `rendered.html`）；② `wiki-link.ts` 存在性判断改批量查 Postgres。五类内容页面+新增 Digest/Tags 两个此前完全不存在的功能全部接入真实数据，搜索从 Pagefind 换成 Postgres 全文搜索（`body_tsv` GIN 索引第一次真正用上），图片改自定义 scheme 改写+独立静态服务端点。新增 `web` Docker 服务（两份 compose 同步），未引入 nginx（明确留给 M7）。**核心卖点验证**：对着真实持久化部署直接 `INSERT`/`UPDATE` 数据库记录，同一个未重建未重启的容器立即反映变化。详见 `docs/milestones/M6-frontend.md` 与 decisions.md。 |
| 2026-07-05 | M7 | **M7（生产化收尾）开发工作完成，进入 7 天连续运行观察期**：讨论中用户主动提出"Temporal 自带定时器能否取代 Celery Beat"，核实 Redis 全代码库唯一用途就是 Celery broker、`temporalio==1.30.0` 原生支持 Schedule Client API 后，**架构调整为 Temporal 原生 Schedule**（`worker.py` 新增 `ensure_pipeline_schedule` 幂等注册），`celery-worker`/`celery-beat`/`redis` 三个容器连同 `backend/beat/` 模块整体退役；`batch_id` 生成从"Celery task 在 workflow 外部生成"改为"workflow 内部用 `workflow.info().start_time` 兜底生成"（同样满足确定性约束）。新增 `postgres-backup` 服务（`infra/scripts/pg_backup.sh`，每日 pg_dump 快照 + 按保留天数清理，决策为不上 WAL/PITR），新增运维手册 `docs/06-runbook.md`。Langfuse/git_export 均决策暂缓（不是遗漏）。**真实批次验证**：`handle.trigger()` 手动触发一次完整批次，`batch_id` 正确从 `workflow.info().start_time` 生成、14 源全部成功、0 enrich 失败、100 条文档写入；备份容器首次启动即成功生成可用 dump 文件，保留策略清理逻辑用构造的过期文件验证通过。验收标准"连续运行 7 天不需要人工干预"是观察期而非开发任务，观察起始 2026-07-05，详见 `docs/milestones/M7-production-hardening.md` 与 decisions.md。 |
| 2026-07-05 | M10（新增） | **新增 Deep Dive 里程碑占位（延后）**：用户提出"Deep Dive 后续做成独立 Temporal 工作流是否合适"，核实旧系统 `40-Deep-Dives/` 后发现这个功能从未真正实现过（空 `.gitkeep`、无 agent、无 phase 集成，不是"已验证规则待迁移"）；旧系统自己"≥7 天 Digest 历史"的设计门槛其实已经达到（8 天）但从未被开发补上。新建 `docs/milestones/M10-deep-dive.md`，状态"延后"，触发条件是新系统 `documents` 表 `doc_type='digest'` 积累足够天数历史（核实时点只有 1 天）。同步更新 `04-roadmap.md` §4 与 `milestones/README.md`。详见 decisions.md。 |
| 2026-07-05 | M8（提前设计） | **M8 迁移设计定稿，提前于原定"M7 验收后"启动**：调研旧 vault（341 个内容文档，`50-Zettel`/`20-Topics`/`10-Daily`/`30-Digests`/`60-Originals`）发现新旧系统时间窗口存在真实重叠（新系统 `original`/`zettel` 的 `doc_date` 是文章发布日而非抓取日）。迁移策略：Original/Zettel/Topic/Digest 冲突时新系统数据为准，只补新系统没有的内容；**Daily 例外**，用户明确要求保留旧系统按天拆分的粒度。Zettel/Topic 的 ID 格式与新系统一致无需转换，Digest 需重排前缀，**Original 的 ID 必须按 `source_url` 重新计算**（旧文件名格式与新系统 `original-<hash>` 体系不同）。已知脏数据（19 处悬空 wikilink、arXiv 脚注误转义成伪链接、YAML 引号风格不统一）记录进迁移设计供导入脚本处理。完整设计见 `docs/milestones/M8-legacy-migration.md`，详见 decisions.md。 |
| 2026-07-05 | M8（执行完成） | **M8 编写迁移脚本并正式执行，用户明确要求"现在就跑"**：`backend/scripts/migrate_legacy_vault.py`（默认 dry-run，`--execute` 才写库）复用 `worker.db`/`worker.aggregate`/`worker.filter` 已有函数。Dry-run 对着真实 vault + 真实 Postgres 验证：341 个文件全部解析成功（修复了 1 个 `title` 字段单引号未闭合的损坏文件），人工抽查确认 topic 合并日期区块正确降序无重复、旧 Original 文件名 wikilink 全部正确重写成新 hash id。执行前手动 `pg_dump` 快照兜底，正式写入 272 条文档（original+122/zettel+106/topic+4/daily+8/digest+7）；Original 净增比计划少 16，核查是旧 vault 自身就有 29 篇重复 `source_url` 的文件（旧系统从未做跨日 URL 去重），upsert 合并是预期行为不是 bug。`links`/`tags` 表外键完整性检查 0 悬空引用，前端抽查 4 个迁移样本全部 200 且渲染正确。M8 状态改为"已完成"，详见 `docs/milestones/M8-legacy-migration.md` 与 decisions.md。 |
| 2026-07-05 | M8（后续清理） | **迁移后数据清理**：核实到两条重复/损坏 Original——`state.ai`→`stateof.ai` SSL 回退未反映到 `source_url` 导致的 State of AI Report 2025 重复归档，以及一份抓取失败、内页为空的 Google Slides 演示文稿占位内容；分别核查 `links` 反查引用数为 0 / 1（且该 1 处引用在前端会优雅降级成"断链"样式）后直接从 `documents` 表删除，不做重抓（该演示文稿本身长期不可抓取）。 |
| 2026-07-05 | M5（追加） | **Daily 主题小标题 emoji + Topic 反链对齐**：对比迁移进来的旧系统 Daily 页面发现新系统主题小标题退化成纯英文 bucket 名，旧系统的 emoji 又是"每天现造"不稳定的做法——改为固定 `TOPIC_EMOJI`/`TOPIC_LABEL` 映射表，`_build_daily_record` 生成 `"## emoji 中文名 [[slug]]"` 格式标题并把 topic slug 补进 `link_targets`（Topic 页可反查 Daily）。已生成的 `2026-07-05` 这篇手动补数对齐新格式，并补上此前因 `2026-07-04` 尚不存在而缺失的"昨日回顾"链接（M8 迁移补齐 07-04 数据后具备条件）。详见 `docs/milestones/M5-aggregate.md`。 |
| 2026-07-05 | M6（追加） | **日期格式修正 + 六个导航列表页分页懒加载 + 渲染性能优化**：`doc_date` 被 `node-postgres` 解析成 JS `Date` 对象导致显示成 `Date.toString()` 格式，驱动层加类型解析器一处修复。真实数据量增长后暴露 Sources 列表页打开要几秒的问题，根因是列表页经过 Live Loader 对每行做整篇 markdown 渲染 + N+1 查询——新增轻量分页查询路径，六个导航列表页（Daily/Digest/Topics/Zettel/Originals/Tags）接入共用的滑动懒加载脚本。过程中修复两个真实 bug：Astro scoped CSS 按源文件加哈希导致懒加载片段丢样式（抽成共享卡片组件解决）；`ClientRouter` 客户端导航后懒加载失效（监听 `astro:page-load` 重新初始化解决，用户报告后经 Playwright 真实点击验证修复）。另外基于实测的超线性渲染耗时数据（46KB/56ms → 合成 20 倍/1.7s），给 Topic 详情页加了 markdown 渲染 LRU 缓存 + 按日期区块懒加载两重优化。详见 `docs/milestones/M6-frontend.md`。 |
| 2026-07-05 | 全量工程审查 | **首次全量工程审查 + 分优先级修复**：M0-M8 完成、M7 观察期中，用户要求"坐下来沉淀项目"，做了一次覆盖 backend/worker、migrations、scripts、tests、frontend、docs/milestones 的全量审查（6 路并行 agent + 人工复核），产出 33 条 finding，随后按严重程度排出任务清单依次修复。**M8 数据修复（最高优先级）**：发现 `migrate_legacy_vault.py` 的 4 个 bug 已经把错误数据写进生产库（Original `doc_date` 误判、Topic `article_count` 语义错误、frontmatter 字段名不对齐导致"0 字/0 条"、`strip_leading_title_block` 正则漏 `re.MULTILINE` 导致正文残留元数据行），patch 脚本 + 新建一次性回填脚本 `repair_m8_legacy_data.py`（dry-run 确认 + `pg_dump` 快照后执行），修正 153 条文档。**严重级**：修 `fetch_original_activity` 异常兜底缺失（真实 404 会让文章从批次消失）、前端 markdown 渲染补 `rehype-sanitize`（白名单额外放行 wikilink data 属性与 Shiki 语法高亮属性）、`pg.Pool` 补 error 监听器、首页改轻量单条查询消除全表 N+1。**重要级**：修 `_slugify` 中文标题 slug 碰撞、`_compute_word_count` 字数注水口径、订正 `04-roadmap.md` 里已被 M1 推翻但未同步的 `tool_choice` 表述、新增 `test_fetch.py`/`test_workflows.py`（真实 Temporal time-skipping WorkflowEnvironment 验证 filter-在-fan-out-前与失败隔离）/`test_db.py`/`test_llm_client.py`（测试套件从 54 条增至 103 条）。**建议级**：`content_hash` 提取共享实现、workflows.py 裸函数调用加"回放安全"标记、`also_reported_by` 死数据清理、tags/topic 详情页体验优化、`test_aggregate.py` 补 `doc_type` 断言等。详见 `.claude/memory/known_issues.md`（待处理项）与本文件对应里程碑文件的追加记录。 |
| 2026-07-06 | M7（观察期真实故障） | **`aggregate_activity` 观察期第二天连续 3 次超时（180s→360s→900s 逐次调大仍不够），当日批次未写入。** 按用户要求停下流程、走完整排查：诊断 workflow 依次排除工作流历史膨胀/线程池饱和/大payload跨 child workflow 传递/共享 HTTP 连接池退化几个假设，`py-spy` 栈追踪确认线程正常阻塞在真实 LLM 网络等待（不是 bug）。切模型到 `qwen3.6-flash` 后一次测试"看似"解决，但坚持看完整不过滤的日志后找到真正根因：`aggregate_activity` 每次都执行成功，但返回值（101 条记录，含 arxiv 全文正文）序列化后达 4,327,723 字节，超过 Temporal gRPC 硬性消息上限（4,194,304 字节），"完成"信号发不出去，从 workflow 角度看像是一直挂起直到超时——这正是 M5 验收时观察到的 `PayloadSizeWarning`（1.16MB）真实触发到了硬上限。修复：`aggregate_activity` 与 `write_activity` 合并为一个 Temporal activity，records 不再跨 Temporal 序列化边界。**真实批次验证**：重建镜像重启后手动触发完整批次（同时补跑当天缺失数据），`sources_attempted=14`/`sources_failed=0`/`fetched=1366`/`kept=95`/`enrich_failed=4`/`written=112`，workflow 正常流转到 `COMPLETED`，日志确认无 gRPC 报错，`documents` 表新增记录数与返回值完全对应；验证通过后恢复了排查期间暂停的 `ainews-pipeline-daily` Schedule。详见 `docs/milestones/M7-production-hardening.md`「观察期真实故障」章节与 `.claude/memory/decisions.md`。 |
| 2026-07-06 | M4/M8（用户报告后排查修复） | **用户报告"arxiv 文章有问题"+"迁移的 daily 原文链接断链"，排查出两个独立真实 bug，外加一个连带发现的严重回归**。① **arxiv 只抓到摘要**：`_fetch_direct` 从 M2-M4 起就只请求 `arxiv.org/abs/<id>` 摘要页（真实验证：全部 83 篇非迁移 arxiv Original 无一例外只有摘要），never 尝试过 arXiv 官方全文 HTML 端点（`/html/<id>`，提交后有生成延迟，实测 3 天后 88% 可用）。修复：`enrich.py` 新增 `_try_arxiv_fulltext` 优先尝试全文端点，静默回退摘要页；连带修了抓取内容自带标题重复渲染（`_ARXIV_LEADING_H1_RE`/`_clean_arxiv_abs_markdown`）、arXiv 官方 logo 图标误抓（`_ICON_SRC_PATTERNS` 补全文页路径模式）、超长无空行段落（参考文献/附录）导致翻译 `IncompleteOutputException` 崩溃（`_chunk_paragraphs` 补硬切）。全文版论文分块数暴增（30-50 块）导致顺序翻译单篇耗时十几分钟，`translate_activity` 分块翻译改并发（`ThreadPoolExecutor`），`workflows.py` 相应超时从 300s/150s 调到 600s/240s。新增一次性脚本 `refetch_arxiv_originals.py`（核对 arXiv API 源头存在性→重抓→重翻译→重算字数/摘要，支持断点续跑）对 83 篇重新处理，平均字数从约 600 提升到 20,402（71 篇命中全文）。② **Daily 原文链接断链**：定位到是 M8 后审查修复引入的**二次回归**——`repair_dailies()`/`repair_digests()` 重算正文时只做了标题剥离，没有重新走 `resolve_links()` 的 wikilink 改写步骤，把已经改写正确的 wikilink 覆盖回断链的旧文件名（3 篇 Daily、133 处链接 100% 断链）；抽出 `build_old_to_new_original_id_mapping()`/`rewrite_original_wikilinks()` 共享函数补上这一步，`repair_m8_legacy_data.py` 天然幂等，重跑一次同时修正现有数据。③ **连带发现的严重回归**：验证 arxiv 图片时发现全站本地图片（`ainews-media://` 自定义协议）在任何页面都不显示——根因是"全量工程审查"新增的 `rehype-sanitize` 不认识这个自定义协议、把 `src` 属性整个清空，`rewriteMediaUrls` 字符串替换在 sanitize 之后跑已经晚了；修复为把替换挪到 sanitize 之前。**用户随后要求对两轮审查的其余改动做一次系统性复查**（担心"通用加固覆盖自定义约定"这类问题不止一处）：本人 + 两个只读 agent 分别核对 M6 前端剩余改动与 M8/全量后端剩余改动，逐条独立验证（不只信任 agent 结论），结论是除已修复的 sanitize 问题外未发现第二处同类回归，两处文档字段漂移（`also_reported_by` 已删除但规格文档仍提及）已顺手订正。详见 `docs/milestones/M4-enrich.md`/`M8-legacy-migration.md` 追加章节与 `.claude/memory/decisions.md`/`known_issues.md`。 |
| 2026-07-07 | M7（观察期真实故障） | **巡检 09:00 自动批次（`kept=94`/`enrich_failed=5`/`written=103`）发现三类问题，均已定位修复，另有两处新发现暂缓**。① 5 篇 arxiv 全文论文（120/88/65/54 分块级别）`translate_activity` 三次重试均撞到 600s 超时——改为分块等待用 `as_completed` + `activity.heartbeat()` 上报进度，`heartbeat_timeout=90s` 判定真卡死，`start_to_close_timeout` 提到 1800s；5 篇重跑全部成功（`word_count` 4.3-7.3 万字符）。② 今天 89 篇 original 里至少 11 篇（约 12%）正文开头重复标题——`_clean_fetched_markdown`（三通道共用清洗管线）新增通用"去掉开头重复 H1"，替代此前只覆盖 arxiv 的专门处理（按用户要求只修代码，不回填历史数据）。③ jiqizhixin 5 条只 1 条入库且内容是微信反爬验证页——`_fetch_jiqizhixin` 提取的直链补 `html.unescape()`（历史 9 篇 4 篇受影响，本次只修代码 + 补今天这 1 篇）；但今天这篇具体文章用两条独立抓取路径重跑仍拿到反爬验证页，确认是微信对内容本身的限制、非我们的 bug，最终按范围删除污染记录未强行补数据。为让重跑文章正确入 Daily/Topic/Digest，顺手修了 `aggregate_activity` 对同一 `batch_id` 重跑时 Topic 文档重复追加的问题（`_build_topic_record` 幂等化，`article_count` 改机械统计）；重新聚合后核实 `documents` 表 93 original+12 topic+1 daily+1 digest=107 条，人工核对确认无同批次重复追加、无跨 topic 重复引用（修复过程中两次重跑 aggregate 触发的 4 篇文章聚类不一致已手动清理）。**新发现、本次未处理**：`normalize_url_for_index` 对 query-string 型 URL（如微信公众号）归一化会碰撞，影响跨日去重准确性；同一篇文章跨天可能被重新聚类到不同 topic 桶（LLM 聚类判断不保证跨天稳定）。两者均需单独设计，详见 `docs/milestones/M7-production-hardening.md`「观察期真实故障」章节与 `.claude/memory/decisions.md`。 |
| 2026-07-08 | M7（观察期，源治理+翻译优化+新架构） | **一整天的连续工作，收官于新增独立的 arxiv 全文回补 workflow**。上午：定位并修复 `huggingface-daily-papers` 连续两天 400 报错（CST/EDT 时区差 + 既有日期回退循环有死角，`raise_for_status()` 在第一次 400 时就抛出、回退到昨天那一步执行不到）；停用 `jiqizhixin`（数据质量问题反复出现）与 `state-of-ai`（历史产出全是静态年度报告页无持续新内容）两个源，新增 `marktechpost`/`venturebeat-ai`/`techcrunch-ai` 三条原生 RSS 替补源，活跃源数 14→13→16→15。中午：`translate_activity` 新增"换 qwen3.6-flash 兜底重试"（真实批次触发 9 次）+ "安全边界切分再合并"两层新兜底（写单测测出并修复一个连带 bug：三层兜底用尽后返回原文，后续 CJK 重试循环没考虑这种情况又发起一次不受保护的调用）；补翻译 29 篇历史降级文章过程中踩了一个 `needs_translation()` 误判坑（拿数据库里已翻译的中文标题当 `Entry.title` 回填，触发"已经是中文不需要翻译"的误判，29 篇全部被跳过翻译，其中 1 篇源 URL 还 404 了）——发现后改用源页面真实标题重新修复，28 篇成功、1 篇（OpenAI 文章）找到公网替代新地址补齐。下午：核查确认 arxiv 全文渲染延迟是 arxiv 自己的固有异步批处理架构（不是可修的 bug），3 篇复查后 2 篇完全修复。**当天最终方案**：核实 huggingface-daily-papers 从来没有全文（只是社区筛选信号）后，把它的 entry 统一改标 `source_name=arxiv-api` 复用 arxiv 处理逻辑；新增 `arxiv_fulltext_pending` 标记（migration 0007）+ 独立的 `ArxivFulltextBackfillWorkflow`（`worker/arxiv_backfill.py`，每天 09:00 Asia/Shanghai 独立 Schedule，只查 14 天窗口候选、只更新 `documents.original`、完全不碰 Topic/Daily/Digest）。真实验证：2 个新增 workflow 单测通过，迁移/镜像/Schedule 均确认生效，人工模拟真实候选手动触发新 workflow 返回 `{"checked": 1, "ready": 1, "upgraded": 1}`，核实 Topic/Daily/Digest 四个相关文档 `updated_at` 全程未变。详见 `.claude/memory/decisions.md` 当天全部条目（从 huggingface 时区修复到 arxiv 回补 workflow 共 6 条决策记录）。 |
| 2026-07-08 | M7（真实09:00批次核查+代码审查修复） | **傍晚核查当天 09:00 真实自动批次，发现并修复一处部署时序缺口，随后做了一次系统性代码审查**。核查 `ainews-pipeline-daily-run-2026-07-08T01:00:00Z`：15 源尝试 14 成功、写入 125 条文档，无重复标题、无 jiqizhixin 残留；但发现 `ArxivFulltextBackfillWorkflow` 镜像中午才重建、晚于 09:00 批次，导致该批次 66 篇 arxiv 来源文章全部没打 `arxiv_fulltext_pending` 标记，其中 10 篇经字数机械校验确认真的只有摘要——已手动补标记，用真实查询函数验证候选数正好命中 10 篇。用户随后要求"审查今天更新的内容有没有漏洞，评估 `enrich.py` 能否抽象"：10 视角并行审查 17 个文件的全部未提交改动，确认 15 条发现后列成 11 项任务清单逐个修复，全部真实验证：① 分块翻译 CJK 复检循环无保护调用 bug（三层兜底重构成策略列表循环 + `ChunkTranslateResult` NamedTuple）；② arxiv 摘要页清洗顺序回归（此前修过一次的渲染问题重新出现，明天回补的 10 篇论文会真实命中，已修复+补回归测试）；③ HuggingFace 点赞信号从未持久化（订正当天早些时候写错的"不丢失"结论）；④ `check_arxiv_fulltext_activity` 改真正轻量检查（不再重复下载配图）；⑤ Temporal replay KeyError 防御；⑥ 回补候选查询改用类型安全布尔列；⑦ 补齐 `M7-production-hardening.md` 缺失的 07-08 章节；⑧ `doc_date` 漂移边界情况（回补现在固定保留既有发布日期）；⑨ `ArxivFulltextBackfillWorkflow` 异常处理/并发/重复判定逻辑三处小修。新增 12 个单测（117→130），重建镜像重启后容器内验证新代码全部正确加载，两个 Schedule 确认仍正确注册。详见 `.claude/memory/decisions.md`「新架构上线当天的一次性标记缺口」与「2026-07-08 代码审查」两条记录。 |
| 2026-07-09 | M10 | **M10（Deep Dive 跨周聚合）设计并落地，状态从"延后"改为已完成**。核查 `documents` 表 digest 历史已达 12 天（超过门槛），用户提出"更适合跑周任务"后走完整 Plan Mode 设计确认（Mermaid 架构图 + 用户显式批准），分 4 步执行：① 后端核心——新增独立 Temporal Schedule `ainews-deep-dive-weekly`（每周一 09:00 Asia/Shanghai）+ `DeepDiveWorkflow` + `backend/worker/deep_dive.py`（趋势统计 activity 机械计算"热门 topic"双门槛 `total_count>=3 且 active_days>=2`，不重新聚类；导语生成 activity 唯一一次 LLM 调用），新增独立 `doc_type='deep_dive'`（无需 migration）。② 单测+冒烟——14 个新用例 + 全量 149 测试通过，容器内用真实窗口数据（451 篇原文）冒烟验证不落库。③ 前端——`deep-dives/index.astro` 从占位页改真实列表页，新建 `more.astro`/`[slug].astro`；类型检查主动发现 `doc-type.ts`/`LuminaBacklinks.astro` 遗漏 `deep_dive` 会导致死链接+反链失效，已补齐。④ 部署验证——重建镜像后首次真实触发即报错，定位到 Temporal 跨 activity 传递未指定字段类型的 dict 时 `date` 字段会退化成字符串（`pydantic_data_converter` 无类型信息可依据解码），修复+补回归测试后重新验证，`documents` 表成功写入首条真实周报 `deep-dive-2026-07-08`（8 个热门话题），真实 HTTP 请求确认列表页/详情页渲染正确、Topic 反链正确显示"深度周报"分组。两处真实 bug（前端 `getLiveEntry` collection key 不匹配、后端 date 类型退化）均由类型检查/真实端到端触发主动发现，不是靠猜测。详见 `docs/milestones/M10-deep-dive.md` 与 `.claude/memory/decisions.md`。 |
| 2026-07-09 | M10（追加，同日） | **用户看了首条真实周报后要求"加粗重点+图标+引入mermaid渲染"，并主动要求"加粗扩大复用到 Daily/Digest"**。先给设计提案经确认后实现：① 加粗改在共享源头 `gist_activity`（`ArticleGist` prompt），一处改动同时让 Daily TL;DR/Daily 主题条目/Digest blurb/Deep Dive 代表文章行四处生效，顺带发现并修复 Digest 硬截断可能切在加粗标记中间的真实问题（新增 `_balance_bold_markers`）；② Deep Dive 统计区块加 emoji；③ 新增 `_build_trend_pie_chart` 机械生成 mermaid 饼图（不经过 LLM），前端新增 `mermaid` npm 依赖 + `MermaidRenderer.astro`（客户端渲染，只接入 Deep Dive 详情页，不是全站默认）。实现前先写独立 Node 脚本实测真实渲染管线，确认 `rehype-sanitize` 白名单完全不需要改动（`code` 默认允许 `language-*` class，`pre` 的 `data-language` 属性 M6 起已在白名单里）。**真实验证**：新增 6 单测（149→154 通过），重建镜像重新触发 `DeepDiveWorkflow` 刷新同一条记录，用 Playwright 真实打开详情页截图确认饼图渲染成带图例的彩色扇形图（不只是"代码能跑通"），控制台 0 报错。详见 `docs/milestones/M10-deep-dive.md`「追加」章节与 `.claude/memory/decisions.md`。 |
| 2026-07-09 | M10（追加二，同日） | **用户反馈加粗还不够、要求深度挖掘更多图表**。加粗力度从"1-2个关键词"提到"2-4处关键词或核心结论短句"；Playwright 实测 4 种候选图表后新增每日产出量柱状图 + 热度/延续性象限图（坐标机械归一化，不经过 LLM）。**象限图首次真实渲染报错，排查定位到一个真实的 mermaid 库解析器 bug**：`quadrantChart` 无法解析形如 `"1.0"` 的浮点数字面量（裸整数 `1` 或非整数小数都正常），而坐标归一化到 (0,1] 区间下批次最高值必然精确等于 1.0——每次都会触发，不是边缘情况。新增 `_format_quadrant_coord` 规避。全量测试 154→159 通过，Playwright 截图确认三张图表全部正确渲染、控制台 0 报错。详见 `docs/milestones/M10-deep-dive.md`「追加二」章节与 `.claude/memory/decisions.md`。 |
| 2026-07-09 | M10（追加三，同日） | **用户指出代表文章摘要无加粗、要求删掉重跑；另反馈图表配色区分度低**。核实"删掉重跑"本身无效——`gist` 是文章 enrich 时一次性生成的历史值，Deep Dive 重跑只读取不重新生成；改为精确定位报告实际引用的 24 篇代表文章单独回填新 prompt 生成的 gist，加粗标记 11→112 处。图表配色改用 `theme:'base'` + 自定义 `themeVariables`（Tableau 10 改良调色板），顺带发现并修正"站内无深色模式"的错误判断（`theme-toggle-client.ts` 证实真实存在），补上明暗双主题适配；配色调试在独立 Playwright CDN 沙盒里一度怎么调都不生效，改到真实 Vite 构建产物里验证一次生效，确认是沙盒环境本身不可靠。Playwright 直接提取真实 SVG `fill` 属性核对配色生效，控制台 0 报错。详见 `docs/milestones/M10-deep-dive.md`「追加三」章节与 `.claude/memory/decisions.md`。 |
| 2026-07-09 | M11（设计确认） | **新增 Topic Deep Dive 专题月报里程碑，走完整设计确认流程后启动实现**：用户看过 M10 周报首批产出后提出"专题月报"想法（如"模型发布专题月报"），核实 `original`/`zettel` 均已带 `topic_slug` 字段、素材现成后给出对话式设计提案（Mermaid 架构图），3 处关键产品判断（doc_type 策略/月度门槛数字/触发时间点）用 `AskUserQuestion` 让用户显式选择。架构上直接复用 M10 已验证的两个模式（`EnrichArticleWorkflow` 的 child workflow fan-out + `DeepDiveWorkflow` 的两段式 activity），复用既有 `doc_type='deep_dive'`（用 `topic_slug` 字段有无区分月报/周报，不新增字段，前端零改动）。新建 `docs/milestones/M11-topic-deep-dive.md`，同步更新 `04-roadmap.md` §4 与 `milestones/README.md`。详见 decisions.md「M11：Topic Deep Dive 设计确认」。 |
| 2026-07-09 | M11（Step1-4 落地） | **M11 分 4 步实现并真实验证**：后端新增 `db.py` 3 个查询函数、`schemas.py` 新 schema、`deep_dive.py` 月度门槛常量+纯函数+3个新 activity、`workflows.py` 新增 parent+child workflow、`worker.py` 注册新 Schedule；单测新增 33 个（全量 189 通过）；前端 `deep-dives` 三个页面适配周报/月报双形态展示。真实部署验证：重建 `temporal-worker`/`web` 镜像，`client.list_schedules()` 确认新 Schedule 注册；手动触发 `TopicDeepDiveWorkflow`（agents，绕过门槛验证）与 `TopicDeepDiveMonthlyWorkflow`（parent，真实门槛筛选出 2 个候选 `industry-moves`/`model-releases`，fan-out 全部成功），0 异常事件；Playwright 确认前端渲染与反链正常。详见 `docs/milestones/M11-topic-deep-dive.md`。 |
| 2026-07-09 | M10+M11（深度叙事引擎重写） | **用户看过真实产出后反馈"太浅薄，像罗列文章不是深度报告"，周报+月报共同触发一次深度改版**：诊断出周报每个热门 topic 小节此前是纯机械 bullet 列表全程无 LLM 参与，月报虽有 LLM 但 prompt 未要求深度分析维度。用户提出协作原则"需要 LLM 增强就用 LLM，机械脚本有局限性"，并追加要求"LLM 能否增强 mermaid 图表"。改进：新增共享 `TopicNarrativeAnalysis` schema（总览+延续性+交叉验证+分歧+新兴信号五维度）+ `_generate_topic_analysis` 共享引擎（周报/月报复用同一函数）；新增"上一期同话题素材"对比机制（Deep Dive 系列第一次让历史产出反过来影响生成）；新增 `TopicRelationshipEdge` 关系图（LLM 标注+代码校验过滤编造边+渲染 mermaid flowchart，项目首次让 LLM 自由文本直接拼进 mermaid 语法，新增 `_sanitize_mermaid_label` 防御性清洗）。周报 `generate_deep_dive_activity` 从 1 次 LLM 调用变成最多 9 次（1 导语+8 话题分析），超时按话题数动态估算。单测新增约 40 个（全量 208 通过），删除首版 3 条浅薄文档重新真实触发验证，Playwright 确认关系图两种边型（印证/矛盾）均正确渲染，控制台 0 报错，周报正文从约 1800 字增至 21055 字。详见 `docs/milestones/M11-topic-deep-dive.md`「追加」章节与 `docs/milestones/M10-deep-dive.md`「追加四」、`.claude/memory/decisions.md`。 |
| 2026-07-09 | M11（追加：全部原文子主题聚类） | **用户对深度叙事引擎仍不满意，精准指出根因"zettel 不是决定性筛选，最多是子话题偏向，应该从 topic 命中的所有原文来规划深度报告"**：核实月报深挖池此前确实是 zettel 反查子集（`industry-moves` 10篇原文仅1篇有zettel），90% 内容未进入报告视野。改进为两级 fan-out：Stage 1 对该 topic 本月全部原文（不经 zettel 过滤）的 title+gist 聚类出 3-7 条真实子主题线索（新增 `TopicCluster`/`TopicClusterResult`），0 条时退化成整个 topic 当一条线索；Stage 2 对每个子主题复用「追加」新建的深度叙事引擎（`_generate_topic_analysis` 参数从 `zettels` 泛化改名 `materials`，周报/月报共用同一实现只换数据源）。深挖全文篇数从"整个topic固定10篇"改成"每子主题独立最多8篇"（新增 `CLUSTER_FULLTEXT_LIMIT=8`，删除 `MONTHLY_FULLTEXT_LIMIT`），总篇数随子主题数自然放大。同批顺带修复用户指出的关系图节点标签用 doc_id 而非标题的问题。单测新增约20个（全量217通过），真实触发验证：`model-releases`（8篇原文）产出4条清晰子主题，全部8篇原文都被深挖到全文（未触及上限），每个子主题的交叉验证/分歧精确到具体文章，素材单薄的子主题正确输出诚实兜底而非编造。详见 `docs/milestones/M11-topic-deep-dive.md`「追加二/追加三」与 `.claude/memory/decisions.md`。 |
| 2026-07-09 | M11（追加：deep_summary 深度内容总结） | **同一"深度不足"反馈第三轮迭代**：用户指出"针对每个方向，在精读原文做五维度的摘要信息，再附上一个篇幅适度的深度的内容总结和报告"。核实根因：`overview` 字段目标只有150-300字，且prompt把全文措辞成"供补充细节参考"，实际生成仍主要依赖标题摘要拼凑。改进：`overview`改名`deep_summary`并升级语义为"篇幅适度的深度内容总结"；新增`summary_length_hint`参数（周报保持150-300字不变，月报显式传600-1200字，两者继续共用同一实现）；`user_content`重排让全文排最前且措辞改为"主要依据"，摘要列表降级为"仅供把握覆盖面"；加粗指引从固定处数改成按篇幅比例。单测新增4个（全量220通过），真实验证`model-releases`月报从4297字增至5739字，深度总结现在具体到定价数字/基准分数/GPU投入规模并做跨文章综合判断，不再是压缩摘要。三轮反馈分别对应"分析深度/素材广度/写作篇幅与素材利用方式"三个不同根因层次，印证"同一方向连续反馈可能指向不同层次"这条经验。详见 `docs/milestones/M11-topic-deep-dive.md`「追加四」与 `.claude/memory/decisions.md`。 |
| 2026-07-09 | M11（追加：篇幅动态化+排版灵活化） | **用户首次正面确认深度总结效果"这次变得非常好了"，同时提出三点未来方向**：丰富区块表达方式/原文多时篇幅调高/每个方向区块延伸可视化。`AskUserQuestion`确认前两点现在实现，第三点因规格不明确留后。改进：`WEEKLY_SUMMARY_LENGTH_HINT`/`MONTHLY_SUMMARY_LENGTH_HINT`固定字符串换成按素材篇数分档的`WEEKLY_SUMMARY_LENGTH_TIERS`/`MONTHLY_SUMMARY_LENGTH_TIERS`+`_dynamic_summary_length_hint`纯函数（月报3档400-700/600-1200/1000-1800字，周报3档150-300/300-500/500-800字）；`summary_length_hint`参数从带默认值改成必填，两处调用方显式算好篇幅再传入；prompt新增排版灵活化指引（允许LLM按内容特点自行决定用小标题/分点）。单测新增约9个（全量223通过），真实验证`industry-moves`（10篇原文分到4个子主题）正文从4158字增至6700字，人工审阅深度总结继续保持高质量。详见 `docs/milestones/M11-topic-deep-dive.md`「追加五」与「留待未来」章节、`.claude/memory/decisions.md`。 |
| 2026-07-09 | M11（追加：mermaid 生成安全网） | **用户提出"不应该完全不信任 LLM 生成 mermaid，而应做好反向约束+lint 兜底"**，结合公开资料（mermaid 官方转义规则、面向 LLM 的提示词工程实践）确认现有"结构化输出+机械模板渲染"架构方向应延续，补强两层防御：① `_sanitize_mermaid_label` 新增 `#` 转义（防止被解析成 HTML 实体前缀）+ `end` 保留字防御；② 新增纯 Python 结构性 `_lint_mermaid_block`（围栏完整/图类型合法/括号引号成对/`quadrantChart` 专属"1.0"浮点 regression 断言），四个图表构建函数返回前自检，未通过时降级为空字符串而非把破损代码块写进正文，两处记录组装函数相应过滤空图表避免留白。`AskUserQuestion` 确认这次只做转义+lint 这一步，dev-only 真实 mermaid.js 金丝雀测试留作后续。单测新增13个（全量236通过）。详见 `docs/milestones/M11-topic-deep-dive.md`「追加六」与 `.claude/memory/decisions.md`。 |
| 2026-07-10 | 生产质量核查 + 翻译降级修复 | **用户要求核查 2026-07-10 真实自动批次（140篇）生成质量**：抽查加粗覆盖率（当天批次跑在加粗prompt改动之前触发，暂无新数据可验证，转而核查上一个已完成批次确认历史规律）、arxiv 回补 workflow 结果（`checked=26,ready=0,upgraded=0`，交叉验证 arxiv 官网 HTML 页确实 404，非误判）、主流水线全程监控（Monitor loop 跟踪 articles 入库数与 workflow 状态，中途修复一次 zsh 只读变量名 `status` 导致脚本崩溃的问题，最终 `written=151` 0 报错完成）。质量核查发现 9/140 篇触发翻译三层兜底的最终降级路径，抽查其中 2 篇（`FedCVESA`/`Latency-Aware Bid Acceptance`）效果很差——"尾块"恰好是参考文献列表而非结论，降级后正文只剩标题+提示+引用列表。**用户确认方向后修复**：新增 `_is_mostly_references`/`_select_fallback_tail_chunk`（`enrich.py`），从后往前找非参考文献分块当尾块，全部尾部都是参考文献时宁可不展示。单测新增6个（全量236→242通过），重建镜像重启生效。 |
| 2026-07-10 | 翻译降级修复 追加：真实回填验证 | **用户主动要求当场重跑回填这 2 篇验证真实效果**（不等 2026-07-11 自然批次）：复用 `ArxivFulltextBackfillWorkflow` 既有模式（`EnrichArticleWorkflow` 真实重跑 + `refresh_original_document_activity` 只更新 `documents.original`）。**第一次重跑结果不对称**——`Latency-Aware Bid Acceptance` 完全修好（711→25771字），但 `FedCVESA` 没有：这篇论文用编号制引用（`[37] Author: Title (2020)`），跟第一版正则依据的"作者[年份]"制完全不同，一条都识别不出来。补齐 `\[\d{1,3}\]`/`doi.org`/裸圆括号年份三个信号后，`FedCVESA` 重跑直接完整通过机械校验（1249→16053字）。单测新增3个（全量242→245通过），两篇文档人工核对确认正文完整连贯。这次真实重跑验证本身就抓出了第一版单测覆盖不到的真实缺口。详见 `.claude/memory/decisions.md`「翻译降级"首尾分块"兜底 追加：真实回填验证暴露编号制引用漏判，两篇论文修复确认」。 |
