# 变量名模板，不含真实值。本地开发时复制成 .env（已 gitignore）。
# 真实密码见 /Volumes/Docker/compose/ainews-service/.env 的 POSTGRES_PASSWORD。

# 只读连接 Postgres ainews_content 库（M6 Live Content Collections 数据源）
DATABASE_URL=postgresql://ainews:<password>@localhost:15432/ainews_content
