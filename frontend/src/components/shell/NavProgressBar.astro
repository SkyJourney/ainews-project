---
// AInews · 顶部导航进度条
// SSR 每次路由切换都要等一次 Postgres 查询 + 渲染往返，ClientRouter 默认没有任何反馈，
// 稍慢时会让点击像"没反应"。用 astro:before-preparation/after-preparation 这对官方事件
// （分别在 fetch 发出前、新页面解析完成后成对触发，正好夹住这段等待窗口）驱动一条细进度条。
// transition:persist 让这个元素在每次导航的 DOM 替换中保留同一个节点，不被新页面里
// "归零"的版本替换掉，否则脚本设置的进行中状态会在 swap 那一刻被打断、看不到效果。
---

<div id="nav-progress-bar" transition:name="nav-progress-bar" transition:persist>
  <div id="nav-progress-bar-fill"></div>
</div>

<style>
  #nav-progress-bar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 3px;
    z-index: 60;
    pointer-events: none;
  }

  #nav-progress-bar-fill {
    height: 100%;
    width: 0%;
    opacity: 0;
    background: linear-gradient(90deg, var(--color-secondary), var(--ai-accent));
    box-shadow: 0 0 8px 1px rgba(78, 222, 163, 0.5);
  }

  @media (prefers-reduced-motion: reduce) {
    #nav-progress-bar-fill {
      transition-duration: 0.01ms !important;
    }
  }
</style>
